CREATE TABLE captura_cliente (
                                 id TEXT PRIMARY KEY UNIQUE NOT NULL,
                                 nome TEXT NOT NULL,
                                 email TEXT NOT NULL
);
